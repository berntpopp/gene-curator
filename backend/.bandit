# Bandit configuration file (YAML format)
# Exclude test files and migrations from security scanning
exclude_dirs:
  - tests
  - alembic

# Skip specific tests that are known false positives
# B101: assert_used - asserts in context managers are acceptable
skips:
  - B101
